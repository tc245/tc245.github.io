{
  "name": "evidence-hub-demo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check",
    "test:unit": "tsx --test tests/*.test.ts",
    "test:e2e": "playwright test",
    "verify": "npm run check && npm run build && npm run test:unit && npm run test:e2e"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  },
  "engines": { "node": ">=22.19.0" }
}
